import StructurePicker from '@/components/Courses/editor/StructurePicker'

// Bare preview of the "Choose your structure" picker (Modules vs Episodic),
// for reviewing the design in isolation before it's wired into the course
// onboarding wizard.
export default function StructurePickerPreviewPage() {
  return <StructurePicker />
}
