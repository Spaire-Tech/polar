import CourseEmptyState from '@/components/Courses/editor/CourseEmptyState'

// Bare preview of the creator-facing course page empty state (Cover hero +
// Free Sample + Spotlight cards + Modules combination, with dark mode).
export default function CourseEmptyStatePreviewPage() {
  return <CourseEmptyState />
}
