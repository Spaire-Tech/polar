import HeroPicker from '@/components/Courses/editor/HeroPicker'

// Bare preview of the "Choose your hero" picker, for reviewing the design in
// isolation before it's wired into the course onboarding wizard.
export default function HeroPickerPreviewPage() {
  return <HeroPicker />
}
