import FreeTrialPicker from '@/components/Courses/editor/FreeTrialPicker'

// Bare preview of the "Let them try it first" picker (Free Preview vs Lesson
// Sample), for reviewing the design in isolation before it's wired into the
// course onboarding wizard.
export default function FreeTrialPickerPreviewPage() {
  return <FreeTrialPicker />
}
