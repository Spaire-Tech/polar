import MarqueeEmptyState from '@/components/Courses/editor/MarqueeEmptyState'

// Bare preview of the creator-facing course page empty state (Marquee hero +
// Episodic + first-3-free + Catalog cards combination, with dark mode).
export default function MarqueeEmptyStatePreviewPage() {
  return <MarqueeEmptyState />
}
