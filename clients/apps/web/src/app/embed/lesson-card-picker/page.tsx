import LessonCardPicker from '@/components/Courses/editor/LessonCardPicker'

// Bare preview of the "Choose your lesson card" picker, for reviewing the
// design in isolation before it's wired into the course onboarding wizard.
export default function LessonCardPickerPreviewPage() {
  return <LessonCardPicker />
}
